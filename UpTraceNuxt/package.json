{
  "name": "UpTrace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "nuxt generate && copyfiles app.yaml dist/ && echo y | gcloud app deploy --project uptraceuofm dist"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.12.5",
    "@nuxtjs/vuetify": "^1.11.3",
    "core-js": "^3.8.3",
    "debounce": "^1.2.1",
    "nuxt": "^2.14.12"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1"
  }
}
